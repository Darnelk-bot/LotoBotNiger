services:
  - type: worker
    name: lotobot-worker
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot_loto.py
    envVars:
      - key: BOT_TOKEN
        value: votre_token_telegram_reel
